- Statement AST for:
  - if-else
  - coroute and defer
  - while and do
  - break and continue
- Binary AST operations for << and >>.
- Binary AST operations for +, -, /, *, ^, and %.
- Equality AST operations for <, >, <=, and >=.
- Function, return, and variable declaraction AST.